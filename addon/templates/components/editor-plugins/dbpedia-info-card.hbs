<div class="modal-dialog__content">
  <div class="wiki-content">
    <h2>Dbpedia info for {{info.term}}</h2>
    {{#if this.image}}
      <img alt="Wikipedia image" src={{this.image}}>
    {{/if}}
    <p>{{this.description}}</p>
  </div>
</div>

{{yield}}
